version: '0.1'
services:
  grpc:
    image: phuochung2599/fullstack:latest
    command: ./entrypoint-grpc.sh
    ports:
      - 50051:50051
    depends_on:
      - postgres
  api:
    image: phuochung2599/fullstack:latest
    command: ./entrypoint-api.sh
    ports:
      - 8080:8080
    depends_on:
      - postgres
  postgres:
    image: postgres:14
    # restart: always
    environment:
        POSTGRES_PASSWORD: 'postgres'
        POSTGRES_USER: 'postgres'
        POSTGRES_DB: 'testdb'  # Add this line
    volumes:
        - ./postgres:/var/lib/postgresql/data
        - ./init:/docker-entrypoint-initdb.d/
    ports:
      - 5432:5432